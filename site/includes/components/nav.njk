<nav id="nav" class="nav flex-l">

    <ul class="menu flex flex-col md:flex-row items-center list-reset text-base">

        {% for item in navigation.main %}

        <li class="level-1 mb-4 md:mr-10 md:mb-0 uppercase text-lg">
            <a href="{{ item.url | safe }}" class="bg-red-200 hover:bg-red-300 text-white hover:text-white font-bold py-4 px-6 rounded"{% if page.url == item.url %} aria-current="page"{% endif %}>{{ item.label }}</a>
        </li>

        {% endfor %}
{#
        <li>
            <a class="inline-block rounded md:ml-2 py-3 px-6 text-white font-bold bg-red-300 hover:bg-blue-300 hover:text-white uppercase text-lg" href="/" target="_blank" rel="noopener">Let's Talk</a>
        </li> #}

    </ul>

</nav>
